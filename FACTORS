
#include <stdio.h>
int main()
{
    int n;
    printf("enter any number");
    scanf("%d",&n);
    printf("Factors of %d are : \n", n);
    
    for(int i = 2; i <= n; i++)
    {
        if(n % i == 0)
            printf(" %d  ", i);
    }
}
